<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/settings_fragment_padding_bottom"
    android:paddingLeft="@dimen/settings_fragment_padding_side"
    android:paddingRight="@dimen/settings_fragment_padding_side"
    android:paddingTop="@dimen/settings_fragment_padding_top" >

    <!-- Wifi switch button -->

    <LinearLayout
        android:id="@+id/wifi_switch_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/checkable_item_bg_selector"
        android:focusable="true"
        android:orientation="horizontal"
        android:padding="8dip" >

        <TextView
            android:id="@+id/wifi_switch_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:text="@string/wifi_switch_title_off"
            android:textColor="#ffffff"
            android:textSize="16sp" />

        <CheckBox
            android:id="@+id/wifi_switch_indicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:clickable="false"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:gravity="center_vertical" />
    </LinearLayout>

    <!-- Wifi scan button -->

    <Button
        android:id="@+id/wifi_scan_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:focusable="true"
        android:gravity="center_vertical"
        android:padding="8dip"
        android:text="@string/wifi_button_scan_text"
        android:textColor="#ffffff"
        android:textSize="16sp" />

    <!-- AP list -->

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:padding="8dip"
        android:text="@string/wifi_access_points"
        android:textColor="#ffffff"
        android:textSize="16sp" />

    <ListView
        android:id="@+id/wifi_ap_list_view"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="@android:color/transparent"
        android:clipToPadding="false"
        android:drawSelectorOnTop="false"
        android:listPreferredItemHeight="48dp"
        android:listSelector="@drawable/list_selector_focused"
        android:paddingBottom="8dp"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="8dp"
        android:scrollbarAlwaysDrawVerticalTrack="true"
        android:scrollbarStyle="outsideOverlay" />

</LinearLayout>